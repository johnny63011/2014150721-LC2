@model IEnumerable<CajeroAutomatico.Entities.Entities.ATM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table  table-bordered table-hover ">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.desAtm)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desAtm)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.atmId }) |
            @Html.ActionLink("Details", "Details", new { id=item.atmId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.atmId })
        </td>
    </tr>
}

</table>
